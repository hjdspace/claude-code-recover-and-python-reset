"""Port of: src/commands/install-github-app/. Install GitHub App."""
from typing import Any
async def call(args: list[str], context: Any) -> dict[str, Any]:
    return {"type": "text", "value": "GitHub App installation (stub)."}
